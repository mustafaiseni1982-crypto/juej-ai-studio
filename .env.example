# Production build (Firebase Hosting → Railway API)
# VITE_API_BASE_URL=https://juej-ai-studio-production.up.railway.app
# (No trailing slash.)

# Railway server env (not Vite): OPENROUTER_API_KEY, OPENAI_API_KEY, PORT, etc.
